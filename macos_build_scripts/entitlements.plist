<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow runtime to work with dynamically loaded libraries -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Allow JIT compilation (needed for some Python libraries) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Allow loading of dynamically linked libraries -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Allow debugging (optional, can remove for production) -->
    <key>com.apple.security.get-task-allow</key>
    <false/>
</dict>
</plist>
